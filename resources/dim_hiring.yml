resources:
  jobs:
    hiring_table:
      name: hiring_table

      run_as:
        user_name: shiv@suraksha555.onmicrosoft.com

      queue:
        enabled: true

      tasks:
        - task_key: hiring_table
          notebook_task:
            notebook_path: ../src/DAB_etl/dim_hiring.ipynb
            
      #     environment_key: hiring_table_environment

      # environments:
      #   - environment_key: hiring_table_environment
      #     spec:
      #       # client: "serverless"
      #       environment_version: "4"
