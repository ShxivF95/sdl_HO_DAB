bundle:
  name: DAB_etl

include:
  - resources/*.yml

# variables:
#   catalog:
#     description: The Unity Catalog to use
#     default: surakshadb

#   schema:
#     description: The schema to use
#     default: bronze_stg

targets:
  dev:
    mode: development
    default: true

    workspace:
      host: https://adb-4471979281132357.17.azuredatabricks.net

    # variables:
    #   # Override schema ONLY for dev
    #   schema: ${workspace.current_user.short_name}
