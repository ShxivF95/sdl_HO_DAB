bundle:
  name: DAB_etl

include:
  - resources/*.yml

variables:
  catalog:
    default: surakshadb

  bronze_schema:
    default: bronze_stg

  silver_schema:
    default: silver_stg

  gold_schema:
    default: gold_curated

    
targets:
  dev:
    mode: development
    default: true

    workspace:
      host: https://adb-7405616458494901.1.azuredatabricks.net

    variables:
      catalog: surakshadb



  prod:
    mode: production

    workspace:
      host: https://adb-7405616458494901.1.azuredatabricks.net
      root_path: /Shared/.bundle/${bundle.name}/${bundle.target} 

    variables:
      catalog: surakshadb_prod

