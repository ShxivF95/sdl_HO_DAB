resources:
  jobs:
    symptoms_table:
      name: symptoms_table

      queue:
        enabled: true

      tasks:
        - task_key: symptoms_table
          notebook_task:
            notebook_path: ../src/DAB_etl/Symptoms_table.ipynb
