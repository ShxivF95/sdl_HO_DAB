resources:
  pipelines:
    bronze_pipeline:
      name: PL_bronze_ingestion_${bundle.target}
      
      libraries:
        - file:
            path: ../src/DAB_etl/etl_bronze.py
      
      catalog: ${var.catalog}
      target: ${var.bronze_schema}      # This is where the NEW Bronze tables will be created
      
      continuous: false
      photon: true 
      serverless: true